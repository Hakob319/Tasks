#include <stdio.h>

int main()
	char sym;

	printf("Enter uppercase word:");
	scanf("%c", &sym);

if (sym >= "A", & sym >= "Z");{


	char lowercase = sym + 32;
	printf("Lowercase %c", lowercase:);{
		else { printf("Wrong,Please input Uppercase:);

		}

		return 0;

	}
